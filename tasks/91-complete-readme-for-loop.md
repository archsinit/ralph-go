# Complete README for loop

Goal: Document loop usage, config, ntfy setup, log layout, and key bindings.

Do:
1. Update README: ralph-go loop usage, plan.md/tasks layout, loop config fields, ntfy setup, log file locations, graceful stop keys, resume behavior.
2. Add a short end-to-end walkthrough (plan -> loop).

Done when:
- README fully documents loop.
- Walkthrough matches real behavior.

Files: README.md
